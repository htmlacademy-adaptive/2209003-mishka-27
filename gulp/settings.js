const src = 'source';
const dest = 'build';
const spriteFiles = `${src}/img/sprite/**/*.svg`;
const extraFiles = ['favicon.ico'];

const settings = {
  src,
  dest,
  spriteFiles,
  extraFiles,
};

export default settings;
